<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <!--新增套餐-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal
        (category_id, name, price, status, description, image, create_time, update_time, create_user, update_user)
        values
        (#{categoryId},#{name},#{price},#{status},#{description},#{image},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>

    <!--分页查询-->
    <select id="pageQuery" resultType="com.sky.dto.SetmealDTO">
        select s.*,c.name category_name from setmeal s left join category c on s.category_id = c.id
        <where>
            <if test="name!=null">
                and s.name like concat('%',#{name},'%');
            </if>
            <if test="categoryId!=null">
                and s.catefory_id=#{categoryId}
            </if>
            <if test="status!=null">
                and status=#{status}
            </if>
        </where>
        order by s.create_time desc
    </select>

    <!--批量删除-->
    <delete id="deleteByIds">
        delete from setmeal where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <!--根据套餐id获取setmeal-->
    <select id="getById" resultType="com.sky.entity.Setmeal">
        select * from setmeal where id=#{id}
    </select>

    <!--修改套餐-->
    <update id="update">
        update setmeal
        <set>
            <if test="setmeal.categoryId != null">
                category_id = #{setmeal.categoryId},
            </if>
            <if test="setmeal.name != null and setmeal.name != ''">
                name = #{setmeal.name},
            </if>
            <if test="setmeal.price != null">
                price = #{setmeal.price},
            </if>
            <if test="setmeal.status != null">
                status = #{setmeal.status},
            </if>
            <if test="setmeal.description != null and setmeal.description != ''">
                description = #{setmeal.description},
            </if>
            <if test="setmeal.image != null and setmeal.image != ''">
                image = #{setmeal.image},
            </if>
            <!-- AOP自动填充更新人、更新时间 -->
            <if test="setmeal.updateTime != null">
                update_time = #{setmeal.updateTime},
            </if>
            <if test="setmeal.updateUser != null">
                update_user = #{setmeal.updateUser}
            </if>
        </set>
        where id = #{setmeal.id}
    </update>

    <update id="startOrStop">
        update setmeal set status=#{status} where id=#{id}
    </update>


        <select id="list" parameterType="Setmeal" resultType="Setmeal">
            select * from setmeal
            <where>
                <if test="name != null"> and name like concat('%',#{name},'%') </if>
                <if test="categoryId != null"> and category_id = #{categoryId} </if>
                <if test="status != null"> and status = #{status} </if>
            </where>
        </select>

<!--根据条件统计套餐数量-->
    <select id="countByMap" resultType="java.lang.Integer">
        select count(id) from setmeal
        <where>
            <if test="status != null"> and status = #{status} </if>
            <if test="categoryId != null"> and category_id = #{categoryId} </if>
        </where>
    </select>



</mapper>
